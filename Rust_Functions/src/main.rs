fn main() {
   


				let x = plus_one(5);

				println!("The value of x is: {x}");
}

fn plus_one(x: i32)-> i32{

/*If you add a semicolon, it becomes a statements, returns no value;If you remove it, returns value */
				x + 1;

}
